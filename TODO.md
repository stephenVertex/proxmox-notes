# Issue tracking

Current work and follow-ups are tracked in Beads (`bd`), as required by
[AGENTS.md](AGENTS.md). The old May backup-planning checklist has been
superseded by the live deployment audit in [BACKUPS.md](BACKUPS.md).

```bash
bd ready
bd list --status=open
```

The documentation refresh is tracked as `proxmox-16d`. Backup coverage,
pruning, service health and retained-VM identity follow-ups are recorded as
separate Beads issues; this page is not a parallel task list.
